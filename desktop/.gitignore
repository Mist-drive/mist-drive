build/bin
node_modules
frontend/dist/*
# Keep a tiny placeholder so `//go:embed all:frontend/dist` compiles
# in CI before Wails runs the Vite build. Real builds overwrite it.
!frontend/dist/index.html
